name: post-to-teams
on:
  workflow_call:
    inputs:
      cardType:
        description: "push | pr"
        required: true
        type: string
    secrets:
      TEAMS_WEBHOOK_URL:
        required: true

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Build Adaptive Card JSON (push)
        if: ${{ inputs.cardType == 'push' }}
        shell: bash
        run: |
          cat > card.json << 'JSON'
          {
            "type": "message",
            "attachments": [
              {
                "contentType": "application/vnd.microsoft.card.adaptive",
                "content": {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  "type": "AdaptiveCard",
                  "version": "1.5",
                  "body": [
                    { "type": "Container", "style": "good", "bleed": true,
                      "items": [
                        { "type": "TextBlock", "text": "⬆️ New Push to **${{ github.ref_name }}**", "weight": "Bolder", "size": "Large", "wrap": true }
                      ]
                    },
                    { "type": "FactSet",
                       "facts": [
                         { "title": "Repository:", "value": "${{ github.repository }}" },
                         { "title": "Commit Message:", "value": "${{ github.event.head_commit.message }}" },
                         { "title": "Pusher:", "value": "${{ github.actor }}" },
                       ]
                     }
                  ],
                  "actions": [
                    { "type": "Action.OpenUrl", "title": "📂 View Compare", "url": "${{ github.event.compare }}" }
                  ]
                }
              }
            ]
          }
            JSON
            
      - name: Build Adaptive Card JSON (push)
        if: ${{ inputs.cardType != 'push' }}
        shell: bash
        run: |
          cat > card.json << 'JSON'
            {
              "type": "message",
              "attachments": [
                {
                  "contentType": "application/vnd.microsoft.card.adaptive",
                  "content": {
                    "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                    "type": "AdaptiveCard",
                    "version": "1.5",
                    "body": [
                      { "type": "Container", "style": "attention", "bleed": true,
                        "items": [
                          { "type": "TextBlock", "text": "🔀 Pull Request Opened", "weight": "Bolder", "size": "Large", "wrap": true },
                          { "type": "TextBlock", "text": "**${{ github.event.pull_request.head.ref }}** → **${{ github.event.pull_request.base.ref }}**", "weight": "Bolder", "wrap": true }
                        ]
                      },
                      { "type": "TextBlock", "text": "“${{ github.event.pull_request.title }}”", "size": "Medium", "weight": "Bolder", "wrap": true },
                      { "type": "FactSet", "facts": [
                          { "title": "Repository", "value": "${{ github.repository }}" },
                          { "title": "PR #", "value": "#${{ github.event.pull_request.number }}" },
                          { "title": "Commit Message:", "value": "${{ github.event.head_commit.message }}" },
                          { "title": "Author", "value": "${{  github.event.pull_request.user.login }}" }
                        ]
                      }
                    ],
                    "actions": [
                      { "type": "Action.OpenUrl", "title": "🔎 View Pull Request", "url": "https://github.com/${{ github.repository }}/pull/${{ github.event.pull_request.number }}" }
                    ]
                  }
                }
              ]
            }
            JSON
            
      - name: Post to Teams
        run: |
          curl -sS -H "Content-Type: application/json" \
            -d @card.json \
            "${{ secrets.TEAMS_WEBHOOK_URL }}"
